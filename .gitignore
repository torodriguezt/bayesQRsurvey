.Rproj.user
.Rhistory
.RData
.Ruserdata
temp_install.*
*_cache/
*_files/
.httr-oauth
.DS_Store
.quarto

# R CMD check / build artefacts
*.Rcheck/
*.tar.gz
Rplots.pdf
revdep/

# R CMD build / install byproducts of the vignette (only vignettes/*.Rmd is
# versioned). Anchored to the root so they do not match man/ or R/ content.
/doc/
/Meta/
inst/doc/

# knitr::spin() byproduct (the plots are embedded in code.html)
figure/

# The JSS submission bundle is built into JSS-submission/ (see below)

# Vignettes are rendered by R CMD build; only the .Rmd is versioned
vignettes/*.html

# Manuscript sources and figures (kept in Overleaf).
# The rules are anchored to the repository root so that they do not also
# match man/figures/, which holds the images shown in README.md.
/*.tex
/*.pdf
/Figures/

# Local development and exploration scripts
check*.R
compare_al.R
investigate_*.R
marcus_approximate.R
reproduce_*.R
articulo.r
simulation_aux_functions.r
MCMC_BWQR_*.R

# JSS submission bundle (rebuilt on demand)
JSS-submission/

# Editor and IDE settings: machine-specific, not part of the package
.vscode/
.idea/

# Build output of the companion Next.js website
.next/
